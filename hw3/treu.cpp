#include "treu.h"

void treu::paintEvent(QPaintEvent *event)
{
    /*QPainter *paint = new QPainter(this);
   paint->setPen(QPen(Qt::black, 4, Qt::DotLine, Qt::RoundCap));
    paint->drawRect(300,300,90,40);
   this->show();

    this->update();*/
}
